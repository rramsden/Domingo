class Domingo.TileMap 

  constructor: ->

  ##
  # load - draws the entire map to a canvas
  
  load: (image) ->
  checkCollision: (object) ->
  update: (layers) ->
  setTileScale: (@scalex, @scaley) ->

  ##
  # @param {Context2D} Cavnas Buffer for drawing tiles

  blit: (buffer) ->
